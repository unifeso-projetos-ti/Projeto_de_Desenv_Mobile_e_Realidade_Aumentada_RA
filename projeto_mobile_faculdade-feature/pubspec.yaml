name: projeto_mobile_faculdade
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  font_awesome_flutter: 10.8.0
  url_launcher: ^6.3.1
  flutter_cube: ^0.1.1
  camera: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Adicione a seção assets para incluir a imagem
  assets:
    - lib/assets/cadeira.jpeg
    - lib/assets/roda.jpg
    - lib/assets/3d_models/
    - lib/assets/3d_models/texturas/
